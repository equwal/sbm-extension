{
  "name": "sbm-extension",
  "private": true,
  "description": "Tests and packaging for the sbm add-on. The add-on itself needs no build.",
  "license": "AGPL-3.0-only",
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "npx --yes web-ext lint --self-hosted=false",
    "build": "npx --yes web-ext build"
  },
  "devDependencies": {
    "fast-check": "^4.3.0"
  }
}
